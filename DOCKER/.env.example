# Copy this to DOCKER/.env on the VM (not committed -- see .gitignore) and
# fill in the real domain once one is pointed at the VM's IP (an A record --
# see DOCKER/README.md step 2). Caddy reads this to know which certificate
# to request from Let's Encrypt and which Host header to route on.
DOMAIN=chess.example.com
